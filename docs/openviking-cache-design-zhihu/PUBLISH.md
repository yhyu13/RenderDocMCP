# 知乎专栏粘贴说明

知乎不渲染 mermaid，也不显示 SVG。本文配图已全部是 PNG，先按顺序在编辑器里上传，再粘贴正文。正文里的 `![…](images/…png)` 不会自动带上本地文件，需要在对应位置插入刚上传的图片。

## 标题

给 RenderDoc 的 MCP 工具加缓存，别把缓存塞进 Python 3.6 扩展里

## 导语（30–50 字，可作开头摘要）

缓存不是记住答案，而是给昂贵的 GPU 读取加一道有边界的旁路。键要带捕获身份，失效要先于命中率。

## 配图上传顺序

1. `images/01-dataflow.png` —— 请求先到响应缓存，命中直接返回，未命中才走 IPC/GPU
2. `images/02-key.png` —— 缓存键 = 捕获身份 + 方法名 + 规范化参数
3. `images/03-categories.png` —— 只读缓存 / 写入失效 / 排空队列绕过三类处理

## 标签建议

RenderDoc、MCP、缓存、OpenViking、图形调试、AI Agent
